/** Large tinted navigation tile with arrow — "Fund III Portfolio", "Fund III Properties". */
export interface PortalLinkProps {
  eyebrow: string;
  title: string;
  href?: string;
  style?: React.CSSProperties;
}
export function PortalLink(props: PortalLinkProps): JSX.Element;
