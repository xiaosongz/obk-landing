/** Horizontal numbered jump strip between hairlines (Buy / Rehab / Rent & stabilization / Refinance / Repeat & scale). */
export interface StageJumpsProps {
  stages: string[];
  hrefBase?: string;
  style?: React.CSSProperties;
}
export function StageJumps(props: StageJumpsProps): JSX.Element;
