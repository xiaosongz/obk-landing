/** Tinted aside with 2px bronze left rule for manager commentary in fund stages. */
export interface ManagerNoteProps {
  eyebrow?: string;
  title?: string;
  children: React.ReactNode;
  style?: React.CSSProperties;
}
export function ManagerNote(props: ManagerNoteProps): JSX.Element;
