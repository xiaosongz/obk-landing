/** Small square status tag — "Portfolio snapshot", "Rented", "Sold". */
export interface TagProps {
  children: React.ReactNode;
  style?: React.CSSProperties;
}
export function Tag(props: TagProps): JSX.Element;
